class Scene4 extends Phaser.Scene {
    constructor(){
        super('nextlevel scene')
    }
//     create() {
//         this.buttonNextLevel = this.add.text(config.width/2, config.height/2, 'Start', { fill: 'rgb(29, 1, 29)' });
//         this.buttonNextLevel.setInteractive()
//         // this.buttonNextLevel.on('pointerover',this.buttonNextLevel.fill('black'))
      
//         this.buttonNextLevel.on('pointerdown',() => {
//             config.index++
//             this.message = MyLevelData[config.index].title
//             console.log(this.message);
//             this.scene.start(this.message); // just restart the same state
//           })
//         // this.scene.start("playGame")
//     // }  
    // buttonNextLevel.addEventListener( () => {
    //     this.levelindex++;
    //     this.state.start('levelstate'); // just restart the same state
    //   }  )
}
